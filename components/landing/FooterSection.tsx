export default function FooterSection() {
  return (
    <footer className="relative z-10 border-t border-white/[0.03] px-6 py-8 text-center">
      <p className="font-mono text-[0.6rem] tracking-[0.1em] text-zinc-600">
        AnimeQuiz — 2025 — Fait avec passion et nostalgie
      </p>
    </footer>
  );
}
